name: Deploy Infrastructure

on:
  push:
    branches: [main]
    paths:
      - "charts/sskd-infrastructure/**"
      - ".github/workflows/deploy-infrastructure.yml"
  workflow_dispatch:
    inputs:
      server:
        description: "Target server"
        required: true
        default: "1"
        type: choice
        options:
          - "1"
          - "2"

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: server-${{ inputs.server || '1' }}
    steps:
      - uses: actions/checkout@v4

      - name: COPY CHARTS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "charts/sskd-infrastructure/*"
          target: "/tmp/sskd-infra"

      - name: DEPLOY
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e

            cd /tmp/sskd-infra/charts/sskd-infrastructure

            helm dependency update

            kubectl create namespace sskd --dry-run=client -o yaml | kubectl apply -f -

            if [ "${{ inputs.server }}" = "2" ]; then
              helm upgrade --install sskd-infrastructure . --namespace sskd -f values-server2.yaml
            else
              helm upgrade --install sskd-infrastructure . --namespace sskd
            fi

            echo "DEPLOYED"

            rm -rf /tmp/sskd-infra
